{
  "name": "insight-bot",
  "version": "1.0.0",
  "description": "Mastra-based chatbot for KPIs and Insights with PostgreSQL",
  "main": "src/index.ts",
  "scripts": {
    "dev": "npx ts-node --transpile-only src/demo.ts",
    "start": "npx ts-node --transpile-only src/demo.ts",
    "test": "npx ts-node --transpile-only src/test-agent.ts"
  },
  "dependencies": {
    "@ai-sdk/google": "^2.0.25",
    "@ai-sdk/openai": "latest",
    "@mastra/core": "^0.23.1",
    "@openrouter/ai-sdk-provider": "^1.2.0",
    "dotenv": "^16.4.0",
    "pg": "^8.11.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pg": "^8.11.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.0"
  }
}
